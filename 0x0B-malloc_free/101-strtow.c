#include <stdlib.h>

/**
 * strtow - turn string into set list of words
 * @str: string
 * Return: list of strings
 */

char **strtow(char *str)
{
    
    char ** retrow;

    if ((str == NULL) || (str == ""))
    {
        return NULL;
    }

    return retrow;

}