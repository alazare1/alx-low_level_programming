#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0
 */

int main(void)
{
	int index = 0;

	while (n >= 0 && n < 10)
	{
		putchar(index + '0');
		index++;
	}
	putchar("/n");

	return (0);
}
